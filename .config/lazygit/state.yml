lastupdatecheck: 1741889350
recentrepos:
    - /home/jeff
startuppopupversion: 5
didshowhunkstaginghint: true
lastversion: 0.61.1
customcommandshistory:
    - git branch -f dut-num-mpls-swap.4 m/master
    - git branch -f dut-num-mpls-swap.3 m/master
    - git branch -f dut-num-mpls-swap.2 m/master
    - a git lint -f
    - git revert --abort
    - DiffviewOpen -C/src/FlowTracker
    - git cherry-pick 2274fad254c3
    - cd /src/FlowTracker
    - echo $NVIM
    - echo $EDITOR
    - a git commit --amend
    - echo $SHELL
    - echo $PWD
    - DiffviewOpen -C /src/SampledFlowTracker
    - DiffviewOpen
    - a git diff
    - a git commit
    - ls
    - ""
    - fish
    - cd /src/Arnet
    - pwd
    - a git status
hidecommandlog: false
githubPullRequests: {}
